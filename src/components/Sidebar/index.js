import React from 'react'

const SidebarContainer = () => {
    return (
        <SidebarContainer>
            <Icon>
                <CloseIcon />
            </Icon>
        </SidebarContainer>
    );
};

export default SidebarContainer;
